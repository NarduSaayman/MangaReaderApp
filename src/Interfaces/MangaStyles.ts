/* eslint-disable no-unused-vars */
// eslint-disable-next-line no-unused-vars
enum MangaStyle {
  CARD = `card`,
  SMALL_CARD = `smallcard`,
  HEADERITEM = `header`,
  PAGE = `page`,
  GENRE = `genre`,
}

export default MangaStyle;
